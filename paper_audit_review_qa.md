# 论文评阅质询与参考回复

本文档基于《数据受限条件下的跨域SAR目标识别方法研究》当前稿件整理，采用 `paper-audit` 的评阅视角组织问题。问题尽量避免过细实现层面的追问，重点围绕论文整体框架、核心方法、实验支撑和工程闭环。下列“参考回复”以论文现有内容为依据，可直接作为答辩准备材料使用，但更建议结合个人表达习惯做口语化调整。

## 1. 论文为什么要把“目标域数据少样本”和“目标域数据不可见”拆成两条技术路线？是否显得重复？

**参考回复：**  
这不是对同一问题的重复展开，而是按照目标域可见性这一关键约束进行场景化划分。第二章解决的是“目标域有少量可访问样本”的问题，因此可以利用少量目标域数据开展域适应；第三章解决的是“训练阶段完全看不到目标域数据”的问题，此时不能再做传统意义上的适配，只能转向域泛化。两章的研究假设、可利用信息和方法设计逻辑都不同，但共同服务于“数据受限条件下的跨域SAR目标识别”这一总目标。第四章又进一步将两类模型统一纳入轻量化部署流程，形成了从场景划分、方法设计到工程落地的完整主线。

**答辩时可强调：**  
论文的主线不是“做了两个算法”，而是“针对两类不同数据约束分别给出最合适的跨域解决方案”。

## 2. 这篇论文的核心创新到底是什么？相比已有跨域SAR识别工作，主要突破在哪里？

**参考回复：**  
本文的核心创新不在于单独引入某一个已有模块，而在于针对不同数据受限场景重新组织跨域识别思路。第二章的突破是把少样本域适应从依赖目标域完整分布统计的全局对齐，转化为“像素级风格迁移 + 判别边界优化”的多层级协同对齐；第三章的突破是面对目标域完全不可见的场景，不再依赖目标域信息，而是通过像素级增强、特征级风格混淆和域对抗学习，从源域内部主动构造域随机化并学习域不变表征；第四章的突破是把前两章得到的跨域模型真正压缩并部署到国产AI芯片平台，打通了“跨域识别方法验证到端侧应用”的链路。因此，本文的创新点体现在“场景化方法设计 + 多层级跨域建模 + 国产化部署闭环”这三个层面。

## 3. 第二章为什么采用“像素级域对齐 + 类混淆域适应”的两阶段设计？这两个模块之间是否存在功能重叠？

**参考回复：**  
这两个模块并不重复，而是分别处理不同层级的域偏移。像素级域对齐模块主要解决仿真SAR与实测SAR在噪声、纹理、背景杂波等底层视觉统计上的差异；类混淆域适应模块主要解决高层判别边界在目标域上的偏移问题，也就是让分类器在目标域特征空间中更清楚地分开各类别。论文中的消融实验能够说明二者的互补关系：在 `1-shot` 场景下，`CUT Only` 达到 `79.35%`，高于 `CC-Loss Only` 的 `71.42%`，说明极少样本时像素级对齐更关键；而在 `all` 场景下，`CC-Loss Only` 达到 `94.18%`，高于 `CUT Only` 的 `91.62%`，说明数据充足时边界优化更有效。完整方法在 `1-shot` 和 `all` 下分别达到 `92.45%` 和 `99.15%`，说明两者之间是递进协同关系，而不是简单叠加。

## 4. 第二章为什么选择 CUT，而不是更常见的 CycleGAN 或传统特征对抗方法？

**参考回复：**  
论文做出这个选择的核心原因，是第二章关注的是目标域样本极少的情况。CycleGAN 等方法本质上更依赖目标域整体分布的估计，目标域数据极少时容易出现模式崩塌；传统特征对抗方法也需要相对稳定的目标域统计信息，否则容易产生错误对齐和负迁移。CUT 的优势在于它通过 PatchNCE 损失最大化输入输出对应图像块的互信息，监督信号更多来自单样本内部的局部结构关系，因此对大规模目标域样本的依赖更低。论文中也给出了较明确的实验支撑：PFDA 和 MACR 所依赖的 CycleGAN 在极少样本设置下出现失效，而本文方法在 `1-shot` 下仍达到 `92.45%`，说明 CUT 更适合少样本域适应场景。

## 5. 类混淆损失在目标域样本极少时会不会不稳定？论文是如何避免“少样本下越学越偏”的？

**参考回复：**  
这是第二章方法中非常关键的风险点，因此论文没有直接使用原始预测结果去构建类混淆，而是加入了三层稳健化设计。第一是温度缩放，用于缓解分类器在目标域上的过度自信；第二是不确定性加权，让更能反映边界模糊性的样本发挥更大作用；第三是类别归一化，降低批内类别不平衡对混淆估计的影响。此外，论文还加入了基于 RandAugment 的一致性正则化，要求输入样本及其增强样本在混淆矩阵层面保持一致。更重要的是，类混淆优化并不是在强烈域偏移的原始输入上孤立进行，而是在 CUT 先完成初步像素级对齐后再开展，因此它面对的是一个已经被拉近的潜在空间，这也是完整方法在 `1-shot` 下仍明显优于单独 `CC-Loss Only` 的原因。

## 6. 第三章在完全看不到目标域数据的前提下，为什么认为这些增强策略真的能提升对真实域的泛化，而不是只是在源域里“自我循环”？

**参考回复：**  
第三章不是做无约束的数据增强，而是围绕 SAR 成像机理去构造与真实域偏移相关的扰动。像素级部分并非任意增强，而是针对实测SAR与仿真SAR之间常见的噪声、频域高频细节和动态范围差异，设计了高斯噪声注入、高频频域扰动和分贝化变换；特征级部分则通过 MixStyle 混合实例级统计量，在中间特征空间主动扩展可能的域风格分布；最后再通过域对抗学习进一步压制这些增强域与原始域之间残留的域特异性信息。也就是说，第三章的方法逻辑是“先在输入层扩展分布，再在特征层打乱风格，最后在判别层学习域不变性”，并不是简单在源域内部重复训练。实验上，这一思路在 SAMPLE 和 S2M-5 两个不同难度的数据集上都成立，准确率分别达到 `96.93%` 和 `93.70%`，说明该方法具有跨数据集的稳定性。

## 7. 第三章为什么一定要把像素级增强、特征级风格混淆和域对抗学习三个模块同时引入？会不会有“模块堆叠”的嫌疑？

**参考回复：**  
如果只是把多个模块机械叠加，通常很难在消融实验中表现出清晰的递进关系。但第三章的实验恰恰说明这三个模块承担的是不同层次的任务。基线模型在 SAMPLE 和 S2M-5 上的准确率分别只有 `48.37%` 和 `37.25%`；仅加像素级增强后，准确率提升到 `81.72%` 和 `74.92%`，说明输入分布扩展解决了基础的过拟合问题；再叠加特征级风格混淆后，提升到 `92.42%` 和 `87.43%`，说明特征分布扩展进一步增强了泛化能力；最后加入域对抗学习后，达到 `96.93%` 和 `93.70%`，说明它主要承担了“收尾式”域不变约束的作用。因此，三者不是无序堆叠，而是“基础支撑 - 表征拓展 - 全局对齐”的递进式设计。

## 8. 第三章为什么把 MixStyle 放在 ResNet50 的前 3 个卷积块之后，而不放到更深层？

**参考回复：**  
论文对这一点的解释是基于特征层级功能的区分。浅层和中层特征更多携带纹理、散射风格、局部统计等域相关信息，因此适合用 MixStyle 去做风格混淆；而最后一个卷积块输出的特征已经更接近类别判别语义，如果在这一层继续混合统计量，可能破坏原有的语义结构，反而损害识别性能。第三章可视化分析也支撑了这一判断：随着网络层数加深，特征逐步从“域特异”转向“语义主导”，因此把风格混淆放在前 3 个卷积块是为了尽量作用于风格信息，而避免破坏最终类别语义。

## 9. 论文如何证明提出的方法不是只对某一个数据集有效，而是具有更一般的跨域意义？

**参考回复：**  
论文对第二章和第三章采取了不同层面的验证。第二章虽然主要基于 SAMPLE 数据集，但构建了从 `all` 到 `1-shot` 的多种目标域样本规模场景，重点证明少样本域适应方法对“数据量变化”具有鲁棒性；第三章则在 SAMPLE 和 S2M-5 两个具有不同域差异强度的数据集上同时验证，重点证明域泛化方法对“跨数据集变化”具有稳定性。特别是第三章中，所提方法在 SAMPLE 上达到 `96.93%`，在更具挑战性的 S2M-5 上仍达到 `93.70%`，跨数据集性能降幅仅 `3.23%`。因此，论文不是只证明某一个固定场景下有效，而是分别从“少样本鲁棒性”和“跨数据集稳定性”两个方向支撑其跨域价值。

## 10. 第四章为什么采用“知识蒸馏 + 后训练量化”的路线，而不是重新设计一个轻量化网络直接训练？

**参考回复：**  
第四章的目标不是重新发明一个端侧模型，而是在尽量保留前两章跨域识别能力的前提下，实现可部署的轻量化压缩。直接从头训练轻量网络，往往会导致前两章已经学到的跨域判别知识丢失，因此论文选择先用 ResNet50 教师模型把跨域知识迁移给 MobileNetV2 学生模型，再通过 INT8 后训练量化进一步压缩模型体积和推理时延。这样做的好处是路径清晰、工程实现成本低，而且便于把第二章和第三章两类模型统一纳入同一部署流程。实验结果也说明这种路线有效：参数量从 `25.6M` 降到 `3.4M`，模型大小从 `45.3MB` 降到 `6.67MB`，C++ 单图时延从 `4.12ms` 降到 `1.33ms`，同时跨域识别精度仍保持在较高水平。

## 11. 轻量化之后性能还是下降了，这样的工程代价是否值得？

**参考回复：**  
需要从“精度损失”和“部署收益”两个维度一起看。就精度而言，第四章中学生模型在两条部署分支上都只出现了有限下降。第二章对应的部署分支在 `all`、`10-shot`、`5-shot`、`1-shot` 下仍分别保持 `96.48%`、`95.63%`、`94.58%` 和 `90.14%`；第三章对应的部署分支在 SAMPLE 和 S2M-5 上仍达到 `96.29%` 和 `91.66%`。就部署收益而言，参数量压缩到原来的约 `1/7`，模型体积降至 `6.67MB`，C++ 单图推理时延降至 `1.33ms`，这对板端部署是非常实质性的提升。因此，这里的结论不是“轻量化零代价”，而是“在可接受的精度代价下获得显著的部署收益”，这正是工程上更重要的平衡。

## 12. 如果评阅专家追问这篇论文目前的边界和不足，应该如何回应？

**参考回复：**  
可以主动承认论文仍然建立在若干合理但有限的假设之上。第一，第二章和第三章都默认源域与目标域共享相同类别空间，因此尚未覆盖开放集识别、部分域适应等更复杂情形；第二，本文的实验主线主要围绕“仿真到实测”的跨域识别展开，虽然已经覆盖少样本和不可见两类典型受限场景，但对更复杂任务分布仍有拓展空间；第三，第四章已经完成国产AI芯片部署验证，但当前仍属于单设备边缘部署范式。对此，论文第五章其实已经给出了较清晰的后续方向，包括开放场景跨域识别、大规模预训练模型、多模态融合以及端云协同推理。这种回答方式既承认边界，又能说明当前工作已经完成了论文目标范围内的闭环验证。

## 第二层进阶质询与专家追问

这一部分的问题比前 12 个更细一层，更接近答辩现场“主问题回答完之后，专家继续往下追问”的场景。

## 13. 第二章为什么把问题定义成“少样本无监督域适应”，而不是“少样本有监督微调”？

**专家可能追问：**
- 既然目标域样本这么少，为什么不直接拿少量标签做微调？
- 你的方法和 few-shot supervised learning 的边界是什么？
- `Target Only` 为什么反而比你的方法差这么多？

**参考回复：**  
第二章的问题设定是“目标域样本可访问，但标签难获取”，这与直接做少样本有监督学习是不同的。对实际SAR任务而言，获取实测图像本身已经困难，而获得可靠标注往往更困难，因此论文更关注“少量无标注目标域样本”的利用问题。`Target Only` 之所以在少样本场景表现较差，本质上是因为它只能依赖极少量目标域标注样本从头拟合目标域判别边界，而本文方法同时利用了源域大量有标签数据与目标域少量无标签样本，能够把源域已有判别知识迁移到目标域，再通过少量目标域样本做分布校正，因此在 `1-shot` 等极端场景下会明显优于 `Target Only`。

## 14. 第二章中为什么不是直接做端到端联合训练，而是先做 CUT，再做 CC-Loss？

**专家可能追问：**
- 两个模块能不能合成一个统一网络？
- 分阶段训练会不会导致误差传递？
- 你有没有做过联合训练和分阶段训练的对比？

**参考回复：**  
论文当前采用分阶段设计，核心考虑是少样本场景下训练稳定性优先。CUT 先在像素层面把仿真域和实测域的底层差异拉近，后续 CC-Loss 再在更一致的潜在空间中优化判别边界，这样更有利于减少少样本下的噪声干扰和错误梯度。如果一开始就在强域偏移条件下同时做图像翻译和边界优化，两部分损失可能彼此干扰，训练更不稳定。  
如果老师继续追问“有没有直接做联合训练对比”，更稳妥的回答是：当前论文主要验证了分阶段路线的有效性，尚未把端到端联合作为独立变量展开系统比较，这可以作为后续优化方向，而不是强行声称已经证明“分阶段一定最优”。

**回答提醒：**  
这一问不要说成“联合训练一定不行”，因为论文里没有直接对照实验。

## 15. 第二章在 `1-shot` 场景下还能达到较高准确率，专家可能会质疑这是不是因为任务本身不够难，你怎么回应？

**专家可能追问：**
- 为什么只有每类 1 张目标域样本还能到 `92.45%`？
- 是不是数据集内部差异本来就不大？
- 结果这么高，是否存在协议偏宽松的问题？

**参考回复：**  
可以从三个层面回答。第一，`1-shot` 并不意味着模型只靠 10 张图训练，它仍然利用了完整源域有标签数据，因此真正起作用的是“源域知识迁移 + 极少量目标域校正”的组合，而不是单纯 few-shot 分类。第二，论文用 `Source Only` 作为基线时准确率只有 `46.29%`，说明仿真到实测之间本身存在明显域偏移，任务并不轻松。第三，`CUT Only`、`CC-Loss Only` 与完整方法之间存在明显差距，说明高结果不是自然出现，而是来自多层级对齐设计的协同作用。  
如果老师继续追问实验协议，回答时应首先明确第二章采用的是怎样的目标域可见性设定，再解释这个设定下结果的含义，不要回避协议问题。

## 16. 第三章的像素级增强为什么选“高斯噪声、高频扰动、分贝化”这三类？这三类增强是否有物理依据？

**专家可能追问：**
- 为什么不是更通用的数据增强，如旋转、裁剪、颜色扰动？
- 这三类增强会不会破坏目标几何结构？
- 你的增强是经验拼接，还是围绕SAR成像机制设计的？

**参考回复：**  
第三章的增强不是从自然图像增广库里任意挑选的，而是尽量贴近 SAR 成像差异来构造。高斯噪声对应实测图像中更复杂的噪声与杂波背景；高频扰动主要模拟不同平台、姿态和成像条件下高频散射细节的变化；分贝化则对应 SAR 幅度图像常见的动态范围压缩和对数变换处理。这三类增强共同目标是扩展源域分布，但尽量保留目标的主要语义结构和散射形态。也就是说，论文不是简单追求“把图变得更花”，而是尝试用更贴近SAR机理的方式构造可能的域变化。

## 17. 第三章的域对抗学习并不是拿真实目标域做对抗，而是拿“原始源域”和“增强域”做对抗，这样真的能学到面向真实域的泛化能力吗？

**专家可能追问：**
- 你对抗的只是人造增强域，不是真实目标域，这样是否存在偏差？
- 如果增强域和真实域差得很远，域对抗还有意义吗？
- 这是不是一种间接的 proxy-domain 对齐？

**参考回复：**  
是的，第三章本质上采用的是一种 proxy-domain 思路。因为目标域在训练阶段完全不可见，论文不能进行真实目标域对齐，因此只能通过构造多个“可能的域变化方向”来扩大源域支持集，并通过域对抗学习迫使模型减少对这些风格差异的敏感性。换句话说，第三章并不是声称“增强域就等于真实域”，而是希望通过增强域覆盖一部分潜在域偏移方向，再借助对抗学习提取对这些变化都相对稳定的特征。因此它的逻辑不是 direct adaptation，而是 domain randomization 加 invariant representation learning。

## 18. 第三章为什么采用单源域泛化设定，而不是多源域泛化？

**专家可能追问：**
- 多源域不是更容易做泛化吗？
- 单源域设定是不是过于理想化？
- 只有一个仿真源域，真的足以支撑对真实域的泛化吗？

**参考回复：**  
论文选择单源域设定，是为了贴近本文关注的“数据受限”条件。对于SAR任务而言，实际情况往往并不是可以轻易获得多个质量一致、类别一致、参数一致的实测源域，相反，更常见的是有一个相对可控的仿真源域，而真实目标域难以访问。因此，单源域泛化并不是放宽问题，而是强调在更受限条件下尽量提升模型外推能力。当然，多源域泛化通常会提供更丰富的训练分布，这也是未来可以继续扩展的方向，但本文的贡献在于证明：即使只有单一仿真源域，通过合理的多层级扰动和对抗学习，也能显著提升面向真实域的泛化能力。

## 19. S2M-5 是自建数据集，专家可能会问它的可信度和代表性，你怎么回答？

**专家可能追问：**
- S2M-5 为什么要自己构建？
- 它和 SAMPLE 的关系是什么？
- 自建数据集会不会让实验更容易“贴着方法设计”？

**参考回复：**  
论文构建 S2M-5 的目的，是为了验证方法在更大域差异下是否仍然有效，而不是只在单一公开数据集上成立。它通过 SAMPLE 中的仿真数据与 MSTAR 中的实测数据组合，选取共有的 5 个类别构成“仿真到实测”的更困难场景。这样做的价值在于，它能把论文第三章的方法放到一个更强的跨域分布偏移下检验，从而补足仅在 SAMPLE 上验证的局限。  
如果老师继续追问“自建数据集是否可能偏向你的方法”，可以回答：S2M-5 不是为了让方法更容易成功，相反从结果看它比 SAMPLE 更具挑战性，因此主要作用是增强对泛化鲁棒性的检验力度。

## 20. 第四章为什么统一选用 MobileNetV2 作为学生网络，而不是分别为第二章、第三章挑选不同轻量模型？

**专家可能追问：**
- 你为什么不比较 ShuffleNet、EfficientNet 或更轻的结构？
- 第二章和第三章的教师模型任务不同，为什么学生网络可以统一？
- 这样会不会牺牲某一条分支的最优性？

**参考回复：**  
第四章强调的是“统一轻量化部署路线”，而不只是单点追求某个模型的最高压缩率。统一采用 MobileNetV2，有三层考虑：一是它相对 ResNet50 已经实现了显著的参数和计算压缩；二是其卷积算子标准化程度高，和 Atlas 200I DK 的模型转换与量化工具链兼容性较好；三是统一学生结构后，第二章与第三章两条部署分支就能在相同部署框架下进行横向对比，从而更清楚地说明本文提出的是一条适用于两类跨域任务的通用部署路线。  
如果老师继续问“是不是最优结构”，可以坦诚回答：论文重点是验证统一部署可行性，而不是穷尽所有轻量骨干网络做全面架构搜索。

## 21. 第四章为什么选择 PTQ，而不是精度通常更高的 QAT？

**专家可能追问：**
- PTQ 的精度通常不如 QAT，为什么还选 PTQ？
- 你是不是为了方便实现而牺牲了精度？
- 如果换成 QAT，结果会不会更好？

**参考回复：**  
第四章选择 PTQ，核心是工程可实施性与部署效率之间的平衡。QAT 需要把量化过程嵌入训练流程并重新训练，成本更高，也会显著拉长从方法模型到部署模型的迭代周期；而 PTQ 只需要少量校准数据即可完成量化，更适合论文第四章强调的“快速形成可部署流程验证”的目标。与此同时，论文中的蒸馏模型在 PTQ 后仍然保持了较好的精度，说明这一路线在当前任务上是可接受的。如果老师进一步问“换成 QAT 会不会更好”，比较稳妥的回答是：从理论上 QAT 可能进一步降低量化带来的精度损失，但本文优先解决的是部署闭环打通和精度-效率平衡问题。

## 22. 专家如果质疑“第四章部署结果和前面章节原始结果为什么不完全一致”，应该怎么应对？

**专家可能追问：**
- 既然第四章说沿用前两章模型，为什么原模型精度数字和前文不同？
- 是不是换了 checkpoint、换了预处理、换了测试口径？
- 如果数字不一致，后面的蒸馏和量化结论还可靠吗？

**参考回复：**  
这一问属于答辩中的高风险问题，最好的方式不是现场临时解释，而是答辩前先把论文中的数字口径完全统一。  
如果现场已经被问到，回答时应抓住一个原则：明确说明第四章的数字是否来自“统一部署链路下的重新评测”，以及它与第二、第三章原始算法实验在模型版本、输入预处理、统计方式、是否板端实测等方面是否存在口径差异。只有把口径说清楚，第四章的数字才有可解释性。  
如果目前文稿里还没有把这件事写透，建议不要强行辩称“完全一致”，而应诚实说明第四章的结果是在统一部署配置下得到的重新统计结果，并在后续版本中进一步补充说明。

**回答提醒：**  
这一问本质上不是口才问题，而是文稿一致性问题，答辩前最好先修正文稿。

## 23. 专家如果质疑“第三章写到了开放世界，但实验其实还是闭集同类空间”，应该怎么回应？

**专家可能追问：**
- 你的实验有没有未知类别？
- 你的方法能不能处理类别空间不一致？
- 为什么摘要里用了“开放世界”这样的表述？

**参考回复：**  
更稳妥的回应方式，是主动把 claim 收回到论文真正验证的范围。可以明确说明：第三章验证的是“目标域不可见、但类别空间一致”的未知域泛化问题，而不是开放集识别或类别空间不一致的广义开放世界问题。论文中使用“开放世界”表述的本意，是强调训练阶段无法访问真实目标域，但从严格学术定义来看，本文实验仍属于闭集条件下的 unseen-domain generalization。  
如果现场问到这一点，最好的答法不是硬解释“开放世界也可以这么理解”，而是主动收缩结论边界，表明你清楚区分了“未知域”和“未知类别”这两个层次。

## 24. 专家如果继续追问结果统计稳定性，比如“为什么没有标准差、置信区间或显著性检验”，应该怎么答？

**专家可能追问：**
- 你说优于现有方法，但差距有些并不算特别大，是否显著？
- 第三章虽然写了十次重复实验，为什么表格只给均值？
- 第二章 few-shot 结果是否受随机抽样影响？

**参考回复：**  
可以分两层回答。第一层是说明论文已经尽量通过多组实验场景、两类数据集和消融实验来增强结论稳健性，不是只依赖单一表格结论。第二层是承认：如果从更严格的统计报告角度看，第三章确实可以进一步补充 `mean ± std` 或置信区间，第二章也可以进一步说明 few-shot 抽样和随机种子控制方式。  
如果老师继续深问，建议直接表态：这个意见是合理的，后续修改版会把统计稳定性报告补完整。这样的回答通常比试图用口头解释替代统计报告更稳妥。

## 25. 专家如果问“你的论文方法和工程部署之间到底是不是一条闭环”，可以怎么把三章关系讲得更紧？

**专家可能追问：**
- 第二章和第三章是两个独立算法，第四章是不是硬拼上去的？
- 第四章为什么不只部署其中性能最好的一个模型？
- 这三章之间的统一性到底体现在哪里？

**参考回复：**  
可以把整篇论文概括为“同一问题链上的三步”。第一步，针对目标域少量可见样本，解决少样本域适应；第二步，针对目标域完全不可见，解决域泛化；第三步，把这两类跨域模型都纳入统一的轻量化压缩与国产化部署框架中，说明前两章不是停留在实验室算法，而是都能够走向端侧实现。第四章之所以同时部署两类模型，而不是只挑一个最好结果的模型，恰恰是为了证明本文提出的不只是某个特定算法，而是一条对“少样本可见场景”和“目标域不可见场景”都成立的工程路线。

## 答辩时建议重点强调的三条主线

1. 本文不是分别做了几个零散模块，而是围绕“目标域可见性”这一关键条件，系统覆盖了少样本域适应和目标域不可见域泛化两类典型场景。
2. 第二章和第三章的共同思想，是把跨域识别从依赖目标域完整统计分布的脆弱对齐，转化为更适合数据受限条件的稳健迁移与域不变表示学习。
3. 第四章的价值在于把前面的算法结果真正落到国产AI芯片平台上，证明论文不是停留在方法有效，而是已经走到了可部署、可验证的工程闭环。

## 使用建议

1. 这份材料更适合作为“答辩准备提纲”，不建议现场逐字照读。
2. 如果现场老师继续深问公式、损失函数或表格细节，可以从本问答中的主线回答自然过渡到第二章、第三章、第四章对应小节。
3. 回答时尽量坚持同一个逻辑顺序：先说明问题场景，再说明方法为什么这样设计，最后落到实验结果和工程价值。
4. 遇到高风险追问时，优先保证口径诚实和边界清晰，不要为了“答得漂亮”而把论文里没有验证的内容说得过满。
