# 第三章实验分析文字优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 优化 `data/chap03.tex` 中对比实验、消融实验、可视化分析的文字分析，深化因果推理、加强与方法设计的对应、修正技术细节。

**Architecture:** 所有改动仅涉及一个文件 `data/chap03.tex` 中的文字段落替换。不新增任何表格、图片或实验数据。保留现有四维度对比分析结构和消融实验配置定义，仅替换分析正文。

**Tech Stack:** LaTeX（中文学位论文）

---

## File Map

- Modify: `data/chap03.tex:422-454` — 对比实验四维度分析正文
- Modify: `data/chap03.tex:497-514` — 消融实验分析正文
- Modify: `data/chap03.tex:545-558` — 数据增强可视化分析正文
- Modify: `data/chap03.tex:585-606` — 消融特征可视化分析正文

---

### Task 1: 修正技术细节问题

**Files:**
- Modify: `data/chap03.tex:497` — 硬编码表引用
- Modify: `data/chap03.tex:453` — 不谨慎用语

- [ ] **Step 1: 修复硬编码表引用**

将第497行的硬编码"表3.5"改为 LaTeX 交叉引用：

```
old: 表3.5所示的消融实验，旨在验证三个核心模块的独立贡献及协同增益效应。
new: 表\ref{tab:transfer-results}所示的消融实验，旨在验证三个核心模块的独立贡献及协同增益效应。
```

- [ ] **Step 2: 修复不谨慎表述**

将第453行"结果不仅优于其他算法"改为更准确的学术表达：

```
old: 结果不仅优于其他算法，
new: 在各项指标上均优于所有对比算法，
```

- [ ] **Step 3: Commit**

```bash
git add data/chap03.tex
git commit -m "fix: 修正第三章硬编码表引用和不谨慎表述"
```

---

### Task 2: 重写对比实验维度(1)——基准实验分析

**Files:**
- Modify: `data/chap03.tex:422-429`

- [ ] **Step 1: 替换基准实验分析段落**

将从 `\textbf{（1）基准实验分析` 到 `50\%左右的准确率仍远未达到实际应用的标准。` 的整段替换为：

```latex
\textbf{（1）基准实验分析：域偏移对识别性能的限制}

如表\ref{tab:comparison-results}所示，在纯仿真数据训练场景下的域泛化SAR目标识别任务中，两种基线模型的性能均明显不足。
ResNet50在SAMPLE和S2M-5数据集上的准确率分别仅为48.37\%和37.25\%，
A-ConvNet分别为52.36\%和44.45\%。
究其原因，未引入任何域泛化策略的通用识别模型在训练过程中倾向于记忆仿真域的特有分布特征（如特定的背景纹理模式与噪声结构），
导致所学习的判别边界过度适配于源域，无法有效迁移至分布差异显著的实测场景。
其中，ResNet50因参数规模较大（约2360万），在仿真数据量有限的条件下更易发生对源域特有模式的过拟合。
相比之下，A-ConvNet作为专门面向SAR图像设计的轻量化网络，其较浅的网络结构和较少的参数量降低了过拟合风险，
对SAR单通道图像的归纳偏置也更为匹配，因此结果略优于ResNet50。
值得注意的是，两种基线在S2M-5上的性能均显著低于SAMPLE（ResNet50降低11.12个百分点，A-ConvNet降低7.91个百分点），
这是因为S2M-5数据集的仿真数据来源于SAMPLE的电磁仿真系统，而实测数据取自MSTAR数据集，
二者在成像平台、观测条件和背景特性上存在更大差异，域偏移程度更为严重。
上述结果表明，在纯仿真数据训练场景下，域偏移是制约模型泛化能力的核心瓶颈，仅依赖标准识别模型无法满足实际应用需求。
```

- [ ] **Step 2: Commit**

```bash
git add data/chap03.tex
git commit -m "refine: 深化对比实验基准分析，增加过拟合机制和跨数据集差异解释"
```

---

### Task 3: 重写对比实验维度(2)——通用域泛化方法分析

**Files:**
- Modify: `data/chap03.tex:431-438`

- [ ] **Step 1: 替换通用域泛化方法分析段落**

将从 `\textbf{（2）通用域泛化方法性能分析` 到 `导致跨域表征的鲁棒性难以进一步突破。` 的整段替换为：

```latex
\textbf{（2）通用域泛化方法性能分析：单层级域泛化策略的局限性}

在引入结合了像素增强的通用域泛化算法后，模型性能获得显著提升。CORAL、DANN、Mixup与VREx在SAMPLE上的准确率均达到87\%以上，
S2M-5上也均超过80\%，表明域泛化策略能够在一定程度上缓解域偏移问题。
然而，这些方法的性能上限仍然受限，其根本原因在于它们各自仅在单一层级上施加域泛化约束，缺乏多层级协同机制。
具体而言，CORAL通过对齐源域与增强域特征的二阶协方差矩阵进行特征级约束，
但缺少像素级的数据分布扩展，当域偏移较大时仅依赖深层特征统计量的对齐难以充分弥合底层视觉差异。
DANN通过域对抗训练引导特征提取器学习域不变表征，其思路与本章所提的域对抗学习模块相近，
但由于缺少像素级数据多样性扩展和特征级风格扰动的支撑，
对抗训练信号的有效性受到源域数据分布单一性的制约。
Mixup在输入空间进行线性凸组合以扩展数据多样性，
但线性插值假设难以适应SAR图像中非线性散射特性产生的复杂分布变化，数据扩展效果有限。
VREx通过最小化不同域上经验风险的方差提供隐式的跨域鲁棒性约束，
其风险外推机制不依赖于特定的域划分方式，因此跨数据集稳定性相对较好（SAMPLE与S2M-5准确率降幅约2.5个百分点）。
然而，各方法在S2M-5上的性能均低于SAMPLE，其中DANN的降幅最为明显（约9.4个百分点），
这进一步表明当域偏移程度增大时，仅在单一层级施加域泛化约束的策略难以有效应对更复杂的跨域分布差异。
```

- [ ] **Step 2: Commit**

```bash
git add data/chap03.tex
git commit -m "refine: 深化通用域泛化方法分析，逐方法分析层级局限和跨数据集差异"
```

---

### Task 4: 重写对比实验维度(3)——SAR专用方法分析

**Files:**
- Modify: `data/chap03.tex:440-446`

- [ ] **Step 1: 替换SAR专用方法分析段落**

将从 `\textbf{（3）SAR专用域泛化方法的性能分析` 到 `说明其特征提取较为保守，牺牲了部分判别能力。` 的整段替换为：

```latex
\textbf{（3）SAR专用域泛化方法的性能分析：跨数据集鲁棒性的不足}

相较于通用域泛化方法，专门针对SAR-ATR范式设计的算法整体性能更高，但不同方法在跨数据集场景下的稳定性差异较为明显。
RN18+以ResNet18为骨干网络，融合了高斯噪声注入、对抗训练与集成学习等多项策略，
其像素级增强思路与本章所提方法具有一定的相似性，
在域偏移相对较小的SAMPLE数据集上取得了94.91\%的准确率。
然而，该方法缺少特征级风格扰动和显式的域对抗学习机制，
当迁移至域偏移更大的S2M-5场景时，集成模型中的多个子网络均在相似的仿真分布上训练，
容易产生同质化的过拟合，导致准确率骤降至74.31\%，跨数据集降幅超过20个百分点。
HDANet引入胶囊网络的部分-整体关系建模机制，
该结构对域变化具有一定的内在鲁棒性，使其跨数据集准确率降幅仅为3.54个百分点，
但其相对保守的特征提取策略限制了模型在单一数据集上的绝对判别力（SAMPLE上仅88.21\%）。
SSR通过高斯混合模型对SAR图像进行目标与杂波的软分割来缩小域偏移，
IRSANet则耦合了杂波抑制模块与域对抗学习，
二者在SAMPLE数据集上均取得了较好的结果（分别为91.69\%和95.76\%），
但在S2M-5上性能降幅较为明显（分别降至81.64\%和88.29\%）。
这是因为S2M-5中的实测数据来自MSTAR，其实测背景更为复杂，
基于杂波抑制的策略难以充分适应跨成像系统带来的背景差异。
尤其值得关注的是，IRSANet在SAMPLE上与本章方法接近（95.76\% vs 96.93\%），
但在S2M-5上差距明显拉大（88.29\% vs 93.70\%），
这表明仅依赖杂波抑制与域对抗的两级策略，在域偏移增大时仍不足以提供充分的跨域鲁棒性。
```

- [ ] **Step 2: Commit**

```bash
git add data/chap03.tex
git commit -m "refine: 深化SAR专用方法分析，解释RN18+跨数据集骤降和杂波抑制局限"
```

---

### Task 5: 重写对比实验维度(4)——本章方法综合分析

**Files:**
- Modify: `data/chap03.tex:449-454`

- [ ] **Step 1: 替换本章方法综合分析段落**

将从 `\textbf{（4）本章方法的综合优势与突破` 到 `说明本章方法具有较好的有效性与鲁棒性。` 的整段替换为：

```latex
\textbf{（4）本章方法的综合优势：多层级协同框架的有效性与鲁棒性}

面对上述问题，本章面向纯仿真数据训练场景，针对SAR图像特性构建了由像素级数据增强、特征级风格混淆和域对抗学习组成的多层级域泛化框架，
从输入分布扩展、特征风格扰动和全局分布对齐三个层面递进式地处理域偏移。
在两个数据集上均取得了最优结果：SAMPLE上准确率达96.93\%、F1-score达96.75\%，S2M-5上准确率达93.70\%、F1-score达93.79\%，
在各项指标上均优于所有对比算法。
消融实验结果（详见\ref{tab:transfer-results}节）进一步表明，三个模块的贡献呈现递进互补关系：
像素级增强为模型提供了分布多样性的基础支撑（消融中准确率提升33--37个百分点），
特征级风格混淆在隐空间层面进一步缓解了域特异性（准确率再提升10--12个百分点），
域对抗学习则通过显式分布对齐消除残留的域偏差（准确率再提升4--6个百分点）。
这一递进结构使得每个模块在前级已有效缓解对应层级域偏移的基础上，针对更深层的残留差异发挥作用。
从跨数据集稳定性来看，本章方法在SAMPLE与S2M-5之间的准确率降幅仅为3.23个百分点，
远低于RN18+的20.60个百分点和DANN的9.40个百分点，
说明多层级协同框架不易因单一层面的域泛化失效而导致整体性能大幅下降，
具备较好的跨场景鲁棒性。
```

- [ ] **Step 2: Commit**

```bash
git add data/chap03.tex
git commit -m "refine: 深化本章方法综合分析，交叉引用消融数据并量化鲁棒性优势"
```

---

### Task 6: 重写消融实验分析

**Files:**
- Modify: `data/chap03.tex:497-514`

- [ ] **Step 1: 替换消融实验分析正文**

将从 `表3.5所示的消融实验` （已在Task 1中修为 `表\ref{tab:transfer-results}所示的消融实验`）到 `说明所提框架的模块设计是有效的。` 的全部分析文字替换为：

```latex
表\ref{tab:transfer-results}所示的消融实验，旨在验证三个核心模块的独立贡献及协同增益效应。

从基准性能来看，当三个模块均未启用时，模型在SAMPLE数据集上的准确率仅为48.37\%、F1-score为45.54\%，
在S2M-5数据集上表现更差（准确率37.25\%、F1-score 33.54\%），
直观体现了仿真与实测数据间固有域偏移对模型泛化能力的严重制约。

仅启用像素级增强模块后，模型性能获得最大幅度的提升：SAMPLE数据集的准确率提升至81.72\%（提升33.35个百分点）、
F1-score达82.95\%，S2M-5数据集的准确率升至74.92\%（提升37.67个百分点）、F1-score达75.61\%。
该模块在S2M-5上的增益更为显著，这是因为S2M-5中仿真数据与实测数据分别来自不同的成像系统，
在背景杂波、噪声模式和动态范围等像素级特征上存在更大差异，
而本章设计的三种增强策略——高斯噪声注入、高频扰动和分贝化变换——分别针对噪声特性、
传感器散射风格和动态范围/乘性噪声进行扰动，协同覆盖了SAR图像主要的像素级域差异维度，
因此在域偏移更大的场景中收益更高。
这一结果表明，像素级增强通过扩展源域数据的分布多样性，
有效缓解了模型对单一仿真分布的过拟合，是整体性能提升的基础支撑模块。

在此基础上进一步叠加特征级风格混合模块后，模型性能继续显著提升：
SAMPLE的准确率升至92.42\%（提升10.70个百分点）、F1-score达91.71\%，
S2M-5的准确率升至87.43\%（提升12.51个百分点）、F1-score达88.25\%。
该模块在S2M-5上的增益同样更大，原因在于像素级增强虽已缓解了表面的分布差异，
但S2M-5中源于不同成像系统的深层特征分布差异仍较SAMPLE更为显著。
MixStyle通过在卷积块1至3的中间特征空间中混合不同增强域样本的实例级统计量（均值和方差），
在像素级增强无法直接触达的特征空间层面进一步扰动域风格信息，
从而补充性地缩小了深层特征分布差异，帮助模型捕获更具泛化性的域不变特征。

在像素级与特征级增强的基础上引入域对抗学习模块后，模型性能达到最优水平：
SAMPLE数据集的准确率与F1-score分别达96.93\%和96.75\%，
S2M-5数据集的准确率与F1-score分别达93.70\%和93.79\%，
其中S2M-5上提升6.27个百分点，增益再次高于SAMPLE（4.51个百分点）。
前两级模块拓展了训练数据的分布多样性并扰动了特征风格，但并未显式约束模型学习域不变表征。
域对抗学习通过梯度反转层与域判别器的博弈机制，为特征提取器施加了显式的分布对齐信号，
从全局层面消除前序模块残留的域特异性偏差，在域偏移更大的场景中这种显式约束的边际收益更高。

综上，三个模块的增益呈现从大到小的递减趋势（像素级$>$特征级$>$对抗学习），
体现了"从粗到精"的递进式域泛化机制：
像素级增强解决最显性的表面分布差异，特征级风格混淆处理中层风格差异，
域对抗学习弥合残留的深层分布偏移。
每个模块的边际效应递减恰好说明前级模块已有效缓解了其所处理层级的域偏移，
而所有模块在S2M-5上的增益均大于SAMPLE，
表明在域偏移更大的场景中多层级协同框架的优势更加显著。
三者从输入扰动扩展、特征表示拓展到分布对齐形成了完整的递进式域偏移处理链路，
其协同作用推动了模型在两个数据集上的性能全面提升，验证了所提框架中各模块设计的有效性与互补性。
```

- [ ] **Step 2: Commit**

```bash
git add data/chap03.tex
git commit -m "refine: 深化消融实验分析，增加因果解释、方法对应和递进关系总结"
```

---

### Task 7: 优化数据增强可视化分析

**Files:**
- Modify: `data/chap03.tex:545-558`

- [ ] **Step 1: 替换数据增强可视化分析正文**

将从 `图\ref{fig:sample_feature_visualization_aba_1}展示了所提方法在底层像素空间的操作效果` 到 `以在保留深层语义结构的前提下对风格扰动进行干预。` 的整段替换为：

```latex
图\ref{fig:sample_feature_visualization_aba_1}展示了所提像素级数据增强方法在底层像素空间的操作效果。
原始源域仿真图像（图3.4a）背景较为纯净，目标轮廓较为平滑，缺少实测SAR图像中常见的复杂退化特征。
经过高斯噪声注入（图3.4b）后，图像背景呈现出更明显的颗粒特征，模拟了实测SAR图像中因复杂散射环境产生的噪声效应，
与\ref{fig:chap3_framework}节中针对仿真与实测SAR图像噪声特性差异的设计动机直接对应；
高频扰动（图3.4c）在目标边缘与强散射点附近引入了高频变化，
旨在模拟不同传感器或成像条件下散射细节的差异，使增强图像在频域特征上更接近实测SAR图像的多样性；
分贝化增强（图3.4d）通过非线性对数变换压缩高亮散射中心的动态范围，
使部分弱散射部件更加可见，同时将SAR图像中固有的乘性噪声（相干斑噪声）转化为近似加性噪声模型，
有助于模型学习更具跨域稳定性的特征表示，这一机制与3.2.2节中引入分贝化增强的设计初衷一致。
上述三种增强策略分别从噪声特性、传感器散射风格和动态范围三个维度扩展了仿真数据的分布覆盖范围，
协同为模型在纯仿真训练条件下学习更具泛化能力的特征表示提供了更丰富的像素级输入支撑。

图\ref{fig:sample_feature_visualization_data}利用t-SNE技术投射了增强数据集在ResNet50网络中四个卷积阶段的特征分布状态，
展示了特征由较强域特异性向更高程度跨域混合的逐层演化过程。
在浅层（图3.5a），网络主要提取低级边缘和纹理特征，对不同增强手段带来的风格变化较为敏感，
各域特征簇呈现出明显的分离状态，说明浅层特征保留了大量与增强方式相关的域特异性信息。
随着网络深度增加，经过卷积块2和块3（图3.5b、图3.5c），
感受野逐步扩大，网络开始捕捉目标的局部组件与结构拓扑，不同域的特征流形出现更多重叠。
到达最深层的卷积块4（图3.5d），网络更侧重于提取高阶抽象语义信息，
不同颜色散点的交叠程度进一步增加，域边界明显减弱。
上述演化过程表明，网络浅层和中层仍保留较多域风格信息，而高层已主要编码语义判别信息。
这一观察为本章3.2.3节中将MixStyle模块部署在卷积块1至3之后的设计决策提供了直观的实验依据：
浅层域特异性最强（图3.5a中各域明显分离），正是风格扰动介入的最佳时机；
而在卷积块4之后不再引入风格混合，是因为该层特征已高度混合（图3.5d），
此时施加风格统计量扰动反而可能破坏已编码的语义判别结构，影响模型的识别性能。
```

- [ ] **Step 2: Commit**

```bash
git add data/chap03.tex
git commit -m "refine: 优化数据增强可视化分析，加强与方法设计动机对应和MixStyle部署论证"
```

---

### Task 8: 优化消融特征可视化分析

**Files:**
- Modify: `data/chap03.tex:585-606`

- [ ] **Step 1: 替换消融特征可视化分析正文**

将从 `\textbf{基线模型：}` 到 `也说明各个模块在本章域泛化SAR目标识别任务中是有效的。` 的所有四段分析替换为：

```latex
\textbf{基线模型：}
如图3.6(a)与图3.7(a)所示，基线模型在未经任何域泛化处理的情况下，表现出明显的域间分离现象。
源域（Simulated）与目标域（Real）的特征分布呈现出显著的割裂状态，二者几乎没有交集，
各类别特征簇较为松散且边界模糊。
这一特征分布形态与消融实验中基线模型的低准确率（SAMPLE 48.37\%、S2M-5 37.25\%）相互印证，
表明模型在仿真数据上学习到的特征过度依赖于源域特定的分布属性，
既未能有效捕捉跨域通用的语义信息，也缺乏将不同类别清晰区分的判别力。

\textbf{仅像素级增强：}
如图3.6(b)与图3.7(b)所示，引入像素级数据增强后，源域与目标域的特征分布开始出现初步的靠近趋势，部分区域产生了少量重叠。
这与消融实验中准确率大幅提升至81.72\%（SAMPLE）和74.92\%（S2M-5）的结果一致，
表明在输入端引入多样化的像素扰动使模型被迫学习对噪声、频域变化和动态范围差异更鲁棒的特征，
从而初步缩小了仿真域与实测域之间的分布差异。
然而，整体上两域特征仍存在较为明显的错位，
且类内特征的聚合度提升有限，说明仅依赖像素级增强尚不足以充分消除深层特征空间中的域特异性信息。

\textbf{像素级 + 特征级增强：}
如图3.6(c)与图3.7(c)所示，在进一步引入特征级风格混合策略后，
代表不同域的红蓝散点距离进一步缩小，源域与目标域特征在宏观上呈现出更高程度的分布接近，
同时不同类别的特征簇呈现出更明显的聚类趋势，类间界限较上一阶段更为清晰。
这一特征空间形态的改善与消融实验中准确率进一步提升至92.42\%（SAMPLE）和87.43\%（S2M-5）的定量结果吻合。
值得注意的是，域间混合度的提升与类别分离度的增强是同步发生的，
这表明MixStyle通过混合不同增强域的特征统计量所实现的隐式风格扰动，
不仅有效减弱了深层特征中携带的域相关信息，还促使模型将更多的表征容量分配给与类别判别直接相关的语义特征，
说明域泛化与判别力提升在该机制下并非对立目标。

\textbf{像素级 + 特征级 + 对抗学习：}
如图3.6(d)与图3.7(d)所示，在上述基础上引入域对抗训练后，
源域与目标域的特征进一步融合，数据来源的可分辨性明显降低，
各类别特征簇表现出较好的内聚性和较清晰的类间分离度。
这一分布形态对应于消融实验中的最优性能（SAMPLE 96.93\%、S2M-5 93.70\%），
表明域对抗学习通过梯度反转层驱动的极大极小博弈，
在前两级模块已有效扩展数据多样性和扰动特征风格的基础上，
进一步从全局层面剥离了残留的域特异性信息，
使模型同时实现了良好的跨域特征对齐与清晰的类别判别边界。
综合四个阶段的可视化结果可以看出，
随着三个模块的逐步叠加，特征空间中的域间混合度与类别分离度同步改善，
这从特征分布的角度直观印证了消融实验中三个模块递进互补的定量结论，
验证了所提多层级域泛化框架中各模块设计的有效性。
```

- [ ] **Step 2: Commit**

```bash
git add data/chap03.tex
git commit -m "refine: 优化消融特征可视化分析，增加定量交叉引用和域混合-类别分离辩证分析"
```

---

## Self-Review Checklist

**Spec coverage:**
- [x] 对比实验维度(1): Task 2
- [x] 对比实验维度(2): Task 3
- [x] 对比实验维度(3): Task 4
- [x] 对比实验维度(4): Task 5
- [x] 消融实验分析深化: Task 6
- [x] 硬编码引用修复: Task 1 Step 1
- [x] "相对提升"误用修正: Task 6（消融分析全部改为"提升XX个百分点"）
- [x] 不谨慎表述修复: Task 1 Step 2
- [x] 数据增强可视化优化: Task 7
- [x] 消融特征可视化优化: Task 8
- [x] 不变部分（数据集介绍、实验设置、表格图片、本章小结）: 未修改 ✓

**Placeholder scan:** 无 TBD/TODO。每个步骤均包含完整 LaTeX 文本。

**Consistency check:**
- 数值引用：所有百分点数值均已与消融表 `tab:transfer-results` 和对比表 `tab:comparison-results` 核验一致
- 术语一致：全文统一使用"像素级/特征级/域对抗学习"三级术语
- "相对提升"已全部改为"提升XX个百分点"或"提升至XX\%"
